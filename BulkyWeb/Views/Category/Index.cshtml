@model List<Category>
<partial name="_Notification"></partial>

<div class="container">
    <div class="row pt-4" pd>

        <div class="col-6">
            <h2 class="text-primary"></h2>
            Category List
        </div>

        <div class="col-6 text-end">
            <a asp-controller="Category" asp-action="Create" class="btn btn-primary">

                <i class="bi bi-plus-circle"></i>
                create new category
            </a>
        </div>

    </div>
<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>
                Category Name
            </th>
            <th>
                Display Order
            </th>
            <th>
                
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var BookData in Model)
        {
            <tr>
                
                <td>
                    @BookData.Name
                </td>
                <td>
                    @BookData.DisplayOrder
                </td>
                    <td>
                        <div class="w-75 btn-group" role="group">
                            <a asp-controller="Category"  asp-action="Edit" asp-route-id="@BookData.Id" class="btn btn-primary mx-2">
                                <i class="bi bi-pencil"></i>Edit
                            </a>

                            <a asp-controller="Category" asp-action="Delete" asp-route-id="@BookData.Id" class="btn btn-danger mx-2">
                                <i class="bi bi-trash"></i>Delete
                            </a>
                        </div>
                    </td>
            </tr>
        }
    </tbody>
</table>

</div>